{
  "name": "sample-expressjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo '=== BUILD ENV ===' && echo NODE_ENV=$NODE_ENV && echo MY_DEBUG_VAR=$MY_DEBUG_VAR && printenv | grep MY_ || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "github.com/digitalocean/sample-expressjs"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">= 18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
